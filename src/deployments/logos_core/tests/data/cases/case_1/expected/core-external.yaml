apiVersion: v1
kind: Service
metadata:
  name: core-external
  namespace: ns
spec:
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8645
  selector:
    app: zerotenkay-core2
  type: NodePort
